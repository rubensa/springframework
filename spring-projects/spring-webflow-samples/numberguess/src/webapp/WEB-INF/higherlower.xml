<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE webflow PUBLIC "-//SPRING//DTD WEBFLOW//EN"
	"http://www.springframework.org/dtd/spring-webflow.dtd">

<webflow id="higherlower" start-state="enterGuess">
	
	<view-state id="enterGuess" view="higherlower.enterGuess">
		<transition on="submit" to="guess"/>
	</view-state>
	
	<action-state id="guess">
		<!-- access the stateful action via a proxy -->
		<action bean="higherlowerGameProxy"/>
		<transition on="invalidInput" to="enterGuess"/>
		<transition on="retry" to="enterGuess"/>
		<transition on="success" to="showAnswer"/>
	</action-state>
	
	<end-state id="showAnswer" view="higherlower.showAnswer"/>

	<!-- the proxy to the stateful higher lower game (a multi action) -->
	<bean id="higherlowerGameProxy" class="org.springframework.webflow.action.StatefulActionProxy">
		<property name="actionAttribute" value="game"/>
		<property name="actionId" value="higherlowerGame"/>
	</bean>

	<!-- note: higherlower is a prototype, it holds caller state in flow scope transparently -->
	<bean id="higherlowerGame" class="org.springframework.webflow.samples.numberguess.HigherLowerGame" singleton="false"/>

</webflow>