<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE webflow PUBLIC "-//SPRING//DTD WEBFLOW//EN"
	"http://www.springframework.org/dtd/spring-webflow.dtd">

<webflow id="detail" start-state="getDetails">

	<action-state id="getDetails">
		<action bean="phonebook" method="getPerson(${flowScope.id})"/>
		<transition on="success" to="displayDetails"/>
		<transition on="error" to="error"/>
	</action-state>

	<view-state id="displayDetails" view="details">
		<transition on="back" to="finish"/>
		<transition on="select" to="browseColleagueDetails"/>
	</view-state>

	<subflow-state id="browseColleagueDetails" flow="detail">
		<attribute-mapper>
			<input value="${sourceEvent.parameters.id}" as="id" from="string" to="long"/>
		</attribute-mapper>
		<transition on="finish" to="getDetails"/>
		<transition on="error" to="error"/>
	</subflow-state>
	
	<end-state id="finish"/>
	
	<end-state id="error" view="error"/>
	
</webflow>